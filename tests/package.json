{
  "name": "echo-tutorial-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test suite for echo-tutorial monorepo",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/?(*.)+(spec|test).js"],
    "testTimeout": 10000
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}